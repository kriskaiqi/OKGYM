# -----------------------------------------------------------------------------
# General Ignored Files
# -----------------------------------------------------------------------------
# OS generated files
.DS_Store           # macOS system files
Thumbs.db           # Windows thumbnail cache files
ehthumbs.db
Desktop.ini
.localized

# Log files
*.log
npm-debug.log* # npm debug logs
yarn-debug.log* # Yarn debug logs
yarn-error.log* # Yarn error logs
.pnpm-debug.log* # PNPM debug logs

# Temporary files / editor backups
*~
*.swp
.fuse_hidden*

# Environment variables
.env                # Common for sensitive config (e.g., API keys, database URLs)
.env.*.local        # Specific local env files
.env.local          # Specific local env file

# IDE specific files (adjust based on your IDE)
.idea/              # IntelliJ IDEA / WebStorm
.vscode/            # Visual Studio Code settings (can be useful to share some configs, but typically ignored)
*.iml               # IntelliJ IDEA modules
.project            # Eclipse
.classpath
.settings/

# -----------------------------------------------------------------------------
# Node.js / JavaScript specific
# -----------------------------------------------------------------------------
# node_modules
node_modules/       # All installed packages for Node.js
# or if you have separate frontend/backend node_modules
# frontend/node_modules/
# backend/node_modules/

# Build artifacts
dist/               # Common output directory for compiled/bundled code
build/              # Another common output directory
.cache/             # Cache directories generated by build tools (e.g., Babel, Webpack, Next.js)
coverage/           # Test coverage reports
out/                # Another common output directory
.next/              # Next.js build output
.nuxt/              # Nuxt.js build output

# Yarn / npm specific lock files (typically you DO want to commit package-lock.json or yarn.lock)
# But if for some reason you don't want them:
# package-lock.json
# yarn.lock
# pnpm-lock.yaml

# -----------------------------------------------------------------------------
# Backend specific (Examples - adjust as needed for your specific backend language)
# -----------------------------------------------------------------------------

# Python specific (if you have a Python backend)
__pycache__/        # Compiled Python bytecode
*.pyc               # Python compiled files
.venv/              # Virtual environments
venv/               # Common virtual environment names
*.egg-info/
.pytest_cache/

# Java specific (if you have a Java backend)
*.class             # Compiled Java classes
*.jar               # Java archive files
*.war               # Web application archive files
*.ear               # Enterprise application archive files
.gradle/            # Gradle build files
build/              # Common Java build output (if not already covered by general 'build/')
target/             # Maven build output

# PHP specific (if you have a PHP backend)
vendor/             # Composer dependencies
composer.lock       # Composer lock file (typically committed, but if you want to ignore)

# Ruby specific (if you have a Ruby backend)
.bundle/            # Bundler dependencies
vendor/bundle/

# -----------------------------------------------------------------------------
# Database related (if using local database files)
# -----------------------------------------------------------------------------
*.sqlite3           # SQLite database files
*.db                # Generic database files (be specific if possible)
*.sql               # Database dumps (usually ignored)

# -----------------------------------------------------------------------------
# Other common ignored files/folders
# -----------------------------------------------------------------------------
*.zip
*.tar
*.gz
*.rar
*.7z
*.bak
*.tmp
*.log
*.DS_Store